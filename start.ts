function create(arr: string[], index) {
    let str: string = "";
    if (index == arr.length - 1) {
        for (let j: number = 0; j <= 4; j++) {
            
        }


        console.log("")
    }
}

let arr1: string[] = ["1", "2", "3", "4"];

for (let i: number = 0; i < arr1.length; i++) {
    create(arr1, i);
}